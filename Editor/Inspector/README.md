# UniGame Build Pipeline Editor

Интегрированный редактор для управления пайплайнами сборки в UniGame. Позволяет создавать, редактировать и запускать пайплайны сборки с удобным интерфейсом.

## Характеристики

### 📋 Основные возможности

- **Просмотр всех доступных пайплайнов** - Список всех пайплайнов в проекте с быстрым поиском
- **Запуск пайплайнов** - Выполнение полных пайплайнов с отслеживанием прогресса
- **Редактирование пайплайнов** - Добавление и удаление шагов, управление активностью шагов
- **Каталог команд** - Просмотр всех доступных команд с описаниями
- **Создание пайплайнов** - Создание новых пайплайнов с конфигурируемым путем
- **Запуск отдельных шагов** - Выполнение отдельных шагов без полного запуска пайплайна
- **Поиск** - Поиск по пайплайнам и шагам
- **Настройки** - Сохраняемые параметры редактора

### 🎨 Технические особенности

- **UI Toolkit** - Современный интерфейс на UI Toolkit
- **Относительные пути для стилей** - Стили работают независимо от расположения пакета
- **Отдельный Assembly** - Код редактора в отдельном asmdef файле
- **ScriptableSingleton** - Настройки хранятся в ProjectSettings
- **Metadata Attributes** - Система описания команд через атрибуты

## Меню и доступ

Редактор доступен через главное меню Unity:

```
UniGame > Uni Build > Pipeline Editor    (Главное окно редактора)
UniGame > Uni Build > Create Pipeline    (Быстрое создание)
UniGame > Uni Build > Find Pipelines     (Поиск всех пайплайнов)
UniGame > Uni Build > Command Catalog    (Каталог команд)
```

## Структура проекта

```
unigame.unibuild/
├── Editor/
│   ├── Inspector/                    # Редактор пайплайнов
│   │   ├── Styles/
│   │   │   └── BuildPipelineEditor.uss
│   │   ├── Editors/
│   │   │   └── BuildPipelineEditorWindow.cs
│   │   ├── Views/
│   │   │   └── TabView.cs
│   │   ├── BuildCommandMetadataAttribute.cs
│   │   ├── BuildCommandDiscovery.cs
│   │   ├── BuildPipelineEditorWindow.cs
│   │   ├── BuildPipelineInspectorSettings.cs
│   │   ├── BuildPipelineMenu.cs
│   │   ├── BuildPipelineSettingsManager.cs
│   │   ├── PipelineExecutionState.cs
│   │   ├── PipelineExecutor.cs
│   │   ├── PipelineManager.cs
│   │   └── unigame.unibuild.pipeline.inspector.asmdef
│   ├── Pipeline/
│   ├── Commands/
│   └── ...
```

## Использование

### Открытие редактора

1. В меню Unity выберите `UniGame > Uni Build > Pipeline Editor`
2. Откроется окно редактора с несколькими вкладками

### Вкладки редактора

#### 📚 Pipelines
Просмотр всех доступных пайплайнов:
- **Поиск** - Быстрый поиск по названию
- **Create Pipeline** - Создание нового пайплайна
- Клик на пайплайн выбирает его для редактирования

#### ✏️ Editor
Редактирование выбранного пайплайна:
- **Поиск по шагам** - Фильтрация шагов
- **Список шагов** - Показывает все шаги пайплайна
- **Кнопки управления**:
  - ✓ Toggle - Активирование/деактивирование шага
  - Run - Запуск отдельного шага
  - Remove - Удаление шага
- **Execute** - Запуск полного пайплайна

#### 📖 Command Catalog
Каталог всех доступных команд:
- Команды организованы по категориям
- Показывает название, описание и тип команды
- Поиск по названию и описанию

#### ⚙️ Settings
Настройки редактора:
- **Pipeline Creation Path** - Путь для создания новых пайплайнов
- **Auto Refresh Pipelines** - Автоматическое обновление списка
- **Max History Size** - Размер истории выполнения
- **Show Command Descriptions** - Показывать описания команд
- **Enable Detailed Logging** - Детальное логирование

## Добавление метаданных к командам

Используйте атрибут `BuildCommandMetadataAttribute` для добавления информации о команде:

```csharp
[BuildCommandMetadata(
    displayName: "Apply Android Settings",
    description: "Применяет настройки для сборки на Android",
    category: "Platform Configuration",
    iconPath: "Assets/Icons/android-icon.png"
)]
public class ApplyAndroidSettingsCommand : SerializableBuildCommand
{
    public override void Execute(IUniBuilderConfiguration buildParameters)
    {
        // Implementation
    }
}
```

### Параметры атрибута

- **displayName** (string) - Отображаемое название команды
- **description** (string) - Подробное описание функции команды
- **category** (string) - Категория для группировки в каталоге
- **iconPath** (string) - Путь к иконке команды (опционально)

## Программный интерфейс

### Загрузка пайплайнов

```csharp
var pipelines = PipelineManager.LoadAllPipelines();
```

### Поиск пайплайнов

```csharp
var results = PipelineManager.SearchPipelines("build", pipelines);
```

### Создание пайплайна

```csharp
var pipeline = PipelineManager.CreatePipeline("Assets/Pipelines", "MyPipeline");
```

### Выполнение пайплайна

```csharp
var executor = new PipelineExecutor(enableDetailedLogging: true);
var state = executor.ExecutePipeline(pipeline);

if (state.Success)
{
    Debug.Log($"Pipeline executed in {state.ExecutionTime:F2}s");
}
```

### Выполнение отдельного шага

```csharp
var executor = new PipelineExecutor();
bool success = executor.ExecuteStep(command);
```

### Обнаружение команд

```csharp
// Получить все команды
var allCommands = BuildCommandDiscovery.FindAllCommands();

// Получить команды по категории
var platformCommands = BuildCommandDiscovery.FindCommandsByCategory("Platform Configuration");

// Получить все категории
var categories = BuildCommandDiscovery.GetAllCategories();

// Поиск команд
var results = BuildCommandDiscovery.SearchCommands("android");
```

## Работа с настройками

### Получение настроек

```csharp
var settings = BuildPipelineSettingsManager.GetSettings();
var creationPath = settings.PipelineCreationPath;
```

### Сохранение настроек

```csharp
var settings = BuildPipelineSettingsManager.GetSettings();
settings.PipelineCreationPath = "Assets/MyPipelines";
BuildPipelineSettingsManager.SaveSettings(settings);
```

### Сброс настроек

```csharp
BuildPipelineSettingsManager.ResetSettings();
```

## События выполнения

```csharp
var executor = new PipelineExecutor();

executor.OnExecutionStarted += (state) => 
{
    Debug.Log($"Started: {state.PipelineName}");
};

executor.OnStepExecuted += (stepName) => 
{
    Debug.Log($"Executed: {stepName}");
};

executor.OnStepFailed += (stepName, exception) => 
{
    Debug.LogError($"Failed: {stepName} - {exception.Message}");
};

executor.OnExecutionCompleted += (state) => 
{
    Debug.Log($"Completed: {state.Success}");
};
```

## Примеры команд с метаданными

```csharp
[BuildCommandMetadata(
    displayName: "Switch Build Target",
    description: "Переключает активную платформу сборки на указанную",
    category: "Build Configuration"
)]
public class SwitchActiveBuildTargetCommand : SerializableBuildCommand
{
    [SerializeField]
    private BuildTarget buildTarget = BuildTarget.StandaloneWindows64;

    public override string Name => "Switch Build Target";

    public override void Execute(IUniBuilderConfiguration buildParameters)
    {
        EditorUserBuildSettings.SwitchActiveBuildTarget(
            EditorUserBuildSettings.activeBuildTargetGroup,
            buildTarget
        );
    }
}
```

## Хранение настроек

Настройки редактора хранятся в файле:
```
ProjectSettings/BuildPipelineEditorSettings.asset
```

Этот файл можно добавить в систему контроля версий для совместного использования настроек команды.

## Стили и кастомизация

Стили редактора находятся в файле `BuildPipelineEditor.uss` и используют CSS-переменные:

```css
:root {
    --color-background: rgb(56, 56, 56);
    --color-text: rgb(200, 200, 200);
    --color-success: rgb(76, 175, 80);
    --color-error: rgb(244, 67, 54);
    --color-primary: rgb(66, 133, 244);
    /* ... */
}
```

Вы можете переопределить эти переменные для кастомизации внешнего вида редактора.

## Логирование

При включении детального логирования в настройках редактор выведет подробную информацию о каждом этапе выполнения:

```
[Build Pipeline] Starting execution of 'MyPipeline'
[Build Pipeline] Executing step: ApplyAndroidSettings
[Build Pipeline] Step completed: ApplyAndroidSettings (0.15s)
[Build Pipeline] Executing step: BuildPlayer
[Build Pipeline] Pipeline 'MyPipeline' completed successfully in 45.32s
```

## Советы и рекомендации

1. **Организуйте команды** - Используйте категории для логической группировки команд
2. **Добавляйте описания** - Полные описания помогут другим разработчикам использовать команды
3. **Создавайте специализированные пайплайны** - Разные пайплайны для разных платформ/конфигураций
4. **Используйте поиск** - Быстрый поиск поможет найти нужный пайплайн или команду
5. **Сохраняйте настройки** - ProjectSettings хранятся в контроле версий для синхронизации команды

## Расширение функциональности

Вы можете расширить функциональность редактора:

1. **Добавить новые вкладки** - Наследуйте `BuildPipelineEditorWindow` и добавьте новые вкладки
2. **Создать утилиты** - Добавьте специализированные утилиты в папку `Inspector`
3. **Кастомизировать стили** - Переопределите стили в `BuildPipelineEditor.uss`

## Лицензия

Часть пакета UniGame.UniBuild

## Поддержка

Для вопросов и предложений обращайтесь в репозиторий проекта.
